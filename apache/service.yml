apiVersion: v1
kind: Service
metadata: 
  name: backendsrv
spec:
  type: ClusterIP
  selector:
    app: deployappback 
  ports:
    - port: 81
      targetPort: 81
      name: tomcat
      protocol: TCP
